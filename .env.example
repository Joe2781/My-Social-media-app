# Development environment example

# JWT signing secret (do NOT use this in production)
JWT_SECRET=dev_secret_change_me

# SQLite file path (for local dev)
DATABASE_PATH=socialx_dev.db

# Server port
PORT=8080
